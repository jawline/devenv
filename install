#!/usr/bin/env bash
set -e

echo "Base Tools"
(cd programming && ./install)

echo "Tmux"
(cd tmux && ./install)

echo "Zsh"
(cd zsh && ./install)

echo "Termite"
(cd termite && ./install)

echo "i3wm"
(cd i3wm && ./install)

echo "Google cloud tools"
(cd google && ./install)

echo "[+] Reboot for shell changes"
