#!/usr/bin/env bash

./install_apt
./install_node
./install_rust

./setup_zsh

./termite/install_termite
./i3wm/install_i3

./google/install_gcloud

cp -fr .vimrc ~/
cp -fr .vim/ ~/
cp -fr .tmux.conf ~/

cd "${CDIR}"
rm -rf "${TDIR}"

cho "[+] Reboot for shell changes"
