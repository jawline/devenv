#!/usr/bin/env bash
set -e

echo "Base Tools"
(cd programming && ./install)

echo "Installing Haskell"
(cd haskell && ./install)

echo "Tmux"
(cd tmux && ./install)

echo "Zsh"
(cd zsh && ./install)

echo "Vim"
(cd vim && ./install)

# Optional - patch termite for Ubuntu 20.04
#echo "Termite"
#(cd termite && ./install)

#Disabled after switch to Regolith
#echo "i3wm"
#(cd i3wm && ./install)

echo "Google cloud tools"
(cd google && ./install)

echo "[+] Reboot for shell changes"
